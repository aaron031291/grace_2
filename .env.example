# Grace app environment example
# Copy to .env and fill with secure values. Do NOT commit your real .env

# --- Security ---
SECRET_KEY=change-me
ACCESS_TOKEN_EXPIRE_MINUTES=15
BCRYPT_ROUNDS=12

# --- Secrets Vault ---
# Optional: Set encryption key for secrets vault
# If not set, a new key will be generated each run
# GRACE_VAULT_KEY=your-base64-encoded-fernet-key

# --- GitHub Integration ---
# Optional: GitHub Personal Access Token for API access
# Without token: 60 requests/hour (unauthenticated)
# With token: 5000 requests/hour (authenticated)
# Create token at: https://github.com/settings/tokens
# Scopes needed: public_repo (for public repositories only)
GITHUB_TOKEN=

# --- Amp API Integration ---
# Optional: Amp API key for AI agent integration
# Get from: https://ampcode.com/settings/api-keys
AMP_API_KEY=

# --- Reddit API ---
# Get credentials from https://www.reddit.com/prefs/apps
# Create an app (script type) and copy the client ID and secret
# REDDIT_CLIENT_ID=your_client_id_here
# REDDIT_CLIENT_SECRET=your_client_secret_here

# --- Database ---
# For local dev you can use sqlite; for prod, use Postgres
# DATABASE_URL=postgresql+asyncpg://user:password@host:5432/grace
# DATABASE_URL=sqlite+aiosqlite:///./grace.db

# --- Self-Healing ---
# Control automatic self-healing behavior
# SELF_HEAL_OBSERVE_ONLY=true   # Monitor and log issues (default: true)
# SELF_HEAL_EXECUTE=false        # Actually execute fixes (default: false)
# Set both to false to disable self-healing entirely
