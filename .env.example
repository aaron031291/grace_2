# Grace app environment example
# Copy to .env and fill with secure values. Do NOT commit your real .env

# --- Security ---
SECRET_KEY=change-me
ACCESS_TOKEN_EXPIRE_MINUTES=15
BCRYPT_ROUNDS=12

# --- Secrets Vault ---
# Optional: Set encryption key for secrets vault
# If not set, a new key will be generated each run
# GRACE_VAULT_KEY=your-base64-encoded-fernet-key

# --- GitHub Integration ---
# Optional: GitHub Personal Access Token for API access
# Without token: 60 requests/hour (unauthenticated)
# With token: 5000 requests/hour (authenticated)
# Create token at: https://github.com/settings/tokens
# Scopes needed: public_repo (for public repositories only)
GITHUB_TOKEN=your_github_token_here

# --- Amp API Integration ---
# Optional: Amp API key for AI agent integration
# Get from: https://ampcode.com/settings/api-keys
AMP_API_KEY=your_amp_key_here

# --- Reddit API ---
# Get credentials from https://www.reddit.com/prefs/apps
# Create an app (script type) and copy the client ID and secret
# REDDIT_CLIENT_ID=your_client_id_here
# REDDIT_CLIENT_SECRET=your_client_secret_here

# --- Database ---
# For local dev you can use sqlite; for prod, use Postgres
# DATABASE_URL=postgresql+asyncpg://user:password@host:5432/grace
# DATABASE_URL=sqlite+aiosqlite:///./grace.db

# --- Self-Healing ---
# Control automatic self-healing behavior
# SELF_HEAL_OBSERVE_ONLY=false   # Monitor and log issues (default: false, execution enabled)
# SELF_HEAL_EXECUTE=true          # Actually execute fixes (default: true)
# Set both to false to disable self-healing entirely

# --- CAPA ---
# Set to 1/true/yes to enable CAPA auto-creation on critical diagnostics
ENABLE_CAPA_AUTOCREATE=0

# --- Google Search API (Web Learning) ---
# Get free API key (100 searches/day): https://developers.google.com/custom-search/v1/overview
# Step 1: Get API key: https://console.cloud.google.com/apis/credentials
# Step 2: Enable Custom Search API: https://console.cloud.google.com/apis/library/customsearch.googleapis.com
# Step 3: Create search engine: https://programmablesearchengine.google.com/
# Without these, DuckDuckGo is used (may hit rate limits with 403 errors)
GOOGLE_SEARCH_API_KEY=your-api-key-here
GOOGLE_SEARCH_ENGINE_ID=your-search-engine-id-here

# --- Storage ---
# Optional: Override auto-detected disk capacity (in TB)
# GRACE_STORAGE_CAPACITY_TB=2.5
