# Grace Docker Environment Configuration
# This file contains environment variables specifically for Docker deployment

# === BASIC CONFIGURATION ===
GRACE_PORT=8000
FRONTEND_PORT=5173
GRACE_ENV=production

# === DATABASE CONFIGURATION ===
# SQLite (default for development)
DATABASE_URL=sqlite:///./databases/grace.db

# PostgreSQL (uncomment and configure for production)
# DATABASE_URL=postgresql://grace:${POSTGRES_PASSWORD}@postgres:5432/grace
# POSTGRES_PASSWORD=changeme_secure_password

# === REDIS CONFIGURATION ===
REDIS_URL=redis://redis:6379

# === BACKEND API CONFIGURATION ===
BACKEND_CORS_ORIGINS=["http://localhost:5173", "http://frontend:5173", "http://localhost:3000"]

# === FRONTEND CONFIGURATION ===
VITE_API_BASE_URL=http://localhost:8000
VITE_BACKEND_URL=http://backend:8000

# === EXTERNAL API KEYS ===
# OpenAI API Key (required for LLM features)
OPENAI_API_KEY=your_openai_api_key_here

# GitHub Token (for repository access)
GITHUB_TOKEN=your_github_token_here

# === MONITORING CONFIGURATION ===
PROMETHEUS_URL=http://prometheus:9090
GRAFANA_URL=http://grafana:3000

# === LOGGING CONFIGURATION ===
LOG_LEVEL=INFO
LOG_FORMAT=json

# === SECURITY CONFIGURATION ===
SECRET_KEY=your_secret_key_here_change_in_production
JWT_SECRET_KEY=your_jwt_secret_key_here

# === MODEL CONFIGURATION ===
# Ollama Configuration (for local LLM models)
OLLAMA_BASE_URL=http://ollama:11434

# === FEATURE FLAGS ===
ENABLE_CHAOS_TESTING=false
ENABLE_ADVANCED_LEARNING=true
ENABLE_AUTONOMOUS_LEARNING=true
ENABLE_REMOTE_ACCESS=true

# === PERFORMANCE CONFIGURATION ===
MAX_WORKERS=4
MEMORY_LIMIT=2GB
CPU_LIMIT=2.0

# === DEVELOPMENT/DEBUG CONFIGURATION ===
DEBUG=false
DISABLE_LEARNING_JOBS=false
CI=false

# === CONTAINER SPECIFIC ===
# These are automatically set by docker-compose but can be overridden
COMPOSE_PROJECT_NAME=grace
COMPOSE_FILE=docker-compose.yml