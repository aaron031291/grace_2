# ONE COMMAND TO RULE THEM ALL - Fix Python and Boot Grace
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser -Force; $pythonCmd = $null; try { py --version >$null 2>&1; $pythonCmd = "py" } catch {}; if (-not $pythonCmd) { try { python --version >$null 2>&1; $pythonCmd = "python" } catch {} }; if (-not $pythonCmd) { try { python3 --version >$null 2>&1; $pythonCmd = "python3" } catch {} }; if (-not $pythonCmd -and (Test-Path "C:\Users\aaron\grace_2\.venv\Scripts\python.exe")) { $pythonCmd = "C:\Users\aaron\grace_2\.venv\Scripts\python.exe" }; if ($pythonCmd) { Write-Host "✅ Found Python: $pythonCmd" -ForegroundColor Green; if ($pythonCmd -like "*grace_2*") { cd "C:\Users\aaron\grace_2" }; & $pythonCmd -c "import fastapi, uvicorn; print('✅ FastAPI available')" 2>$null; if ($LASTEXITCODE -eq 0) { Write-Host "🚀 Starting Grace..." -ForegroundColor Cyan; & $pythonCmd boot_grace.py } else { Write-Host "Installing FastAPI..." -ForegroundColor Yellow; & $pythonCmd -m pip install fastapi uvicorn requests; & $pythonCmd boot_grace.py } } else { Write-Host "❌ No Python found. Installing..." -ForegroundColor Red; start ms-windows-store://pdp/?productid=9NRWMJP3717K }